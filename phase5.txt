import streamlit as st

st.divider()

# PHASE 5 - WINNER INTELLIGENCE LAYER
st.header("🏆 AI Safety Winner Intelligence Layer")

w1, w2, w3, w4 = st.columns(4)

with w1:
    st.metric(
        "⏱ Early Warning Lead Time",
        "45 Minutes"
    )

with w2:
    st.metric(
        "🎯 Risk Detection Accuracy",
        "97.7%"
    )

with w3:
    st.metric(
        "🛡 False Negative Reduction",
        "82%"
    )

with w4:
    st.metric(
        "👷 Worker Protection Score",
        "96%"
    )

st.subheader("🧠 AI Explainability Engine")

st.info(
    """
    AI Decision Trace:
    
    1. Gas sensor detected abnormal concentration
    2. Maintenance activity identified
    3. Hot work permit conflict detected
    4. Worker PPE violation detected
    5. Historical incidents matched using RAG
    6. Emergency response automatically initiated
    
    Final Decision:
    Compound Risk is HIGH
    
    Reason:
    Multiple weak signals combined into one strong prediction.
    """
)

st.subheader("🤖 Multi-Agent AI Architecture")

agents = [
    ("📡 IoT Sensor Agent", "ONLINE"),
    ("🧠 Compound Risk Agent", "ACTIVE"),
    ("📄 Permit Intelligence Agent", "ACTIVE"),
    ("👁 Computer Vision Agent", "ONLINE"),
    ("📚 RAG Safety Agent", "ACTIVE"),
    ("🚨 Emergency Orchestrator", "READY"),
    ("📋 Compliance Agent", "MONITORING")
]

for agent, status in agents:
    st.success(f"{agent} : {status}")

st.subheader("🌍 Industrial Impact Simulation")

impact1, impact2, impact3 = st.columns(3)

with impact1:
    st.metric(
        "Potential Incident Prevented",
        "127"
    )

with impact2:
    st.metric(
        "Response Time Reduction",
        "70%"
    )

with impact3:
    st.metric(
        "Safety Intelligence Level",
        "Enterprise"
    )

st.subheader("🚀 Zero Harm Mission")

st.success(
    """
    Preet Safety AI transforms traditional reactive safety systems
    into predictive autonomous safety intelligence.

    Instead of detecting accidents after failure,
    the system identifies dangerous combinations before escalation.

    Goal:
    ZERO HARM INDUSTRIAL OPERATIONS
    """
)